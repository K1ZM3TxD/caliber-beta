          {/* Anchor metrics block */}\n          <div className="mt-8 text-left text-[14px] leading-relaxed opacity-80 space-y-2">\n            <div>\n              <span className="font-semibold">Anchor overlap: </span>\n              {typeof (session.synthesis?.anchor_overlap_score ?? session.synthesis?.anchorOverlapScore ?? session.anchor_overlap_score) === 'number'\n                ? ((session.synthesis?.anchor_overlap_score ?? session.synthesis?.anchorOverlapScore ?? session.anchor_overlap_score) as number).toFixed(2)\n                : '—'}\n            </div>\n            <div>\n              <span className="font-semibold">Missing anchors: </span>\n              {typeof (session.synthesis?.missing_anchor_count ?? session.synthesis?.missingAnchorCount ?? session.missing_anchor_count) === 'number'\n                ? (session.synthesis?.missing_anchor_count ?? session.synthesis?.missingAnchorCount ?? session.missing_anchor_count)\n                : '—'}\n            </div>\n            {((session.synthesis?.missing_anchor_terms ?? session.synthesis?.missingAnchorTerms ?? session.missing_anchor_terms) as string[] | undefined)?.length ? (\n              <div>\n                <span className="font-semibold">Missing terms: </span>\n                {((session.synthesis?.missing_anchor_terms ?? session.synthesis?.missingAnchorTerms ?? session.missing_anchor_terms) as string[]).join(', ')}\n              </div>\n            ) : null}        \n          </div>